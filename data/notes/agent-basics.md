# Agent 基础

AI Agent 是一个在环境中观察、选择工具、执行动作并根据结果继续决策的系统。模型只是 Agent 的推理组件，工具、状态、控制循环和评估同样重要。

## 建议顺序

先实现结构化输出和单次工具调用，再手写有最大步数限制的 Agent loop。只有当确定性流程不足时，才增加规划、反思或多 Agent 协作。

## 可靠性

每个工具都需要清晰描述、输入校验、超时和结构化错误。高风险写操作必须经过人工批准。
